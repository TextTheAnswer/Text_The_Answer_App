import 'package:flutter/material.dart';

class AppColors {
  // Default theme
  static const Color primary = Color(0xFFD32F2F);
  static const Color accentOrange = Color(0xFFF4A261);
  static const Color darkGray = Color(0xFF1D3557);
  static const Color lightGray = Color(0xFFA8DADC);
  static const Color white = Color(0xFFFFFFFF);

  static const Color secondary = Colors.blue;

  static const Color error = Color(0xFFFFC107);

  static const Color buttonPrimary = Colors.blue;
  static const Color buttonSecondary = Color(0xFF35383F);
  static const Color buttonTertiary = Color(0xFF1F222A);
  
  // Dark Theme Colors
  static const Color darkOutlineBg = Color(0xFF9489F5); // Outline for text fields in dark mode
  static const Color darkPrimaryBg = Color(0xFF1A1F24); // Bg for text form fields in dark mode
  static const Color darkLabelText = Color(0xFF95A1AC); // Text in the textfield in dark mode
  static const Color darkPrimaryText = Color(0xFFFFFFFF); // Text in dark mode (white)
  static const Color darkBackground = Color(0xFA101213); // Background in dark mode
  static const Color darkOnBoard1 = Color(0xFFC6FFE1); // Onboarding highlight in dark mode
  
  // Light Theme Colors
  static const Color lightOutlineBg = Color(0xFF7B61FF); // Outline for text fields in light mode
  static const Color lightPrimaryBg = Color(0xFFFFFFFF); // Bg for text form fields in light mode
  static const Color lightLabelText = Color(0xFF5F6368); // Text in the textfield in light mode
  static const Color lightPrimaryText = Color(0xFF000000); // Text in light mode
  static const Color lightBackground = Color(0xFFF9F9FB); // Background in light mode
  static const Color lightOnBoard1 = Color(0xFF2A9D8F); // Onboarding highlight in light mode
}
